linters:
  rubocop:
    display_cop_names: true
    # indicate where is the config file for stylelint
    config: './rubocop.yml'
    csslint: {  }
files:
  ignore:
    - 'bower_components/*'
    - 'node_modules/*'
     - "bin/*"
    - "db/*"
    - "config/*"
    - "Guardfile"
    - "Rakefile"


